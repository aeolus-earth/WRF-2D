# Sample configuration file for comprehensive WRF experiments
# Copy this to config.yaml and modify the paths as needed

# WRF installation directory
wrf_dir: "/home/x-yyang40/kangen-wrf-33-cpy/"

# Output directory for experiments
output_dir: "/anvil/scratch/x-yyang40/perturbation_experiments"

# Job submission script
submit_script: "/home/x-yyang40/kangen-wrf-33-cpy/test/ensemble/submit_wrf_no_namelist.sh"

# Directory containing autoencoder model files
model_dir: "/home/x-yyang40/models"

# Perturbed inputs configuration
perturbed_inputs:
  # Directory containing perturbed WRF input files
  # This should be relative to the ensemble/gen_ens directory
  perturbed_dir: "/home/x-yyang40/perturbed_inputs_v2"
  
  # Base run directory to copy from
  base_run: "/anvil/scratch/x-yyang40/run0001_base"
  
  # Whether to submit jobs automatically
  submit_jobs: true
  
  # Suffix for output directory names
  output_suffix: "comprehensive"

# Hidden dimension for autoencoder models (used in partial ensemble)
hidden_dim: 256


